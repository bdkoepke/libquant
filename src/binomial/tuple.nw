<<tuple.h>>=
#pragma once
#include <stdlib.h>
@
<<tuple.c>>=
#include "tuple.h"
typedef struct {
  void *first, *second;
} Tuple;

Tuple *tuple_new(void *first, void *second) {
  Tuple *t = malloc(sizeof(Tuple));
  t->first = first;
  t->second = second;
  return t;
}
@
